// Auth Controller
const {
  authenticateAdmin,
  authenticateCustomer,
  findUserByToken,
} = require('../../database/index');

const isAuthenticated = async (req, res, next) => {
  try {
  } catch (error) {}
};

module.exports = {};
